"""Standalone CLI command modules (report, resume, history, config).

Empty in Phase 2 — ``web`` and ``proto`` are defined directly on the root
app in :mod:`autofuzz.cli.app` as stubs until their engines exist (Phase 4
and Phase 3/5 respectively). This package is populated in Phase 7.
"""
