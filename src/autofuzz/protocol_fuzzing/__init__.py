"""Protocol Fuzzing Engine: FSM-guided mutation fuzzing of live targets.

Generalized successor to v1's ``autofuzz.py``. Implemented across Phase 3
(scheduler/lifecycle/target controller) and Phase 5 (FSM, mutators, FTP
adapter, crash classifier).
"""
